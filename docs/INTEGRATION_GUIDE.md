# 🔗 Полная интеграция: ChatGPT ↔ Rube ↔ FrogFace

**Простое объяснение как все работает и как все соединить!**

---

## 🎯 Главная идея

**Три агента работают вместе:**
1. **ChatGPT** - принимает голосовые команды от тебя
2. **Rube** - автоматизирует процессы и создает рецепты
3. **FrogFace** - показывает задачи в игровом формате с наградами

---

## 📊 Схема работы

```
┌─────────────────────────────────────────────────────────┐
│                    ТЫ (Человек)                          │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ Голосовая команда:
                  │ "Создай задачу: Добавить фичу X"
                  ▼
┌─────────────────────────────────────────────────────────┐
│              ChatGPT Desktop App                        │
│  • Слушает голосовые команды                            │
│  • Распознает намерения                                 │
│  • Преобразует в структурированные данные              │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ POST /api/ai/messages
                  │ { from_agent: "chatgpt", 
                  │   to_agent: "rube",
                  │   type: "task_request",
                  │   content: "Создать задачу: Добавить фичу X" }
                  ▼
┌─────────────────────────────────────────────────────────┐
│              AI Coordination Hub                        │
│              (Supabase ai_messages)                      │
│  • Хранит сообщения между AI                            │
│  • Отслеживает статусы задач                            │
│  • Обеспечивает синхронизацию                           │
└──────────┬──────────────────────┬───────────────────────┘
           │                      │
           │ Читает сообщения     │ Читает сообщения
           ▼                      ▼
┌──────────────────────┐  ┌──────────────────────┐
│      Rube Agent      │  │   FrogFace Frontend  │
│  (Composio/Recipes)  │  │  (Веб интерфейс)     │
│                      │  │                      │
│  • Читает из         │  │  • Показывает задачи │
│    ai_messages       │  │  • Принимает отметки │
│  • Создает задачи    │  │  • Обновляет статусы │
│  • Автоматизирует    │  │  • Синхронизируется  │
│  • Отвечает в        │  │  • Обновляет UI      │
│    ai_messages       │  │                      │
└──────────┬───────────┘  └──────────┬───────────┘
           │                         │
           │ POST /api/tasks          │ PATCH /api/tasks
           │ Создает задачу           │ Обновляет задачу
           │                         │
           ▼                         ▼
┌─────────────────────────────────────────────────────────┐
│              Supabase Database                          │
│  • tasks - игровые квесты                               │
│  • ai_messages - сообщения AI                          │
│  • ai_tasks - задачи между AI                          │
│  • ai_knowledge - база знаний                          │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Полный цикл работы

### **1. Создание задачи через ChatGPT**

```
ТЫ → ChatGPT: "Создай задачу: Добавить авторизацию"
       ↓
ChatGPT → AI Hub: POST /api/ai/messages
         {
           from_agent: "chatgpt",
           to_agent: "rube",
           type: "task_request",
           content: "Создать задачу: Добавить авторизацию",
           priority: "high"
         }
       ↓
Rube читает: GET /api/ai/messages?to_agent=rube&status=pending
       ↓
Rube создает: POST /api/tasks
             {
               title: "Добавить авторизацию",
               priority: "high",
               projectId: "frogface",
               reward: 300
             }
       ↓
Supabase сохраняет в таблицу `tasks`
       ↓
FrogFace Frontend синхронизируется: GET /api/sync
       ↓
ТЫ видишь задачу в интерфейсе! 🎉
```

### **2. Выполнение задачи**

```
ТЫ → FrogFace Frontend: Отмечаешь задачу как выполненную (чекбокс)
       ↓
Frontend → API: PATCH /api/tasks
          {
            taskId: "task-123",
            completed: true
          }
       ↓
Supabase обновляет: tasks.completed = true
       ↓
Frontend обновляет UI:
  • Добавляет награду в totalPot
  • Увеличивает completedToday
  • Показывает уведомление: "Задача выполнена! +300₽"
       ↓
Rube может проверить: GET /api/ai/tasks?status=completed
       ↓
Rube уведомляет ChatGPT: POST /api/ai/messages
                        {
                          from_agent: "rube",
                          to_agent: "chatgpt",
                          type: "task_response",
                          content: "Задача 'Добавить авторизацию' выполнена! +300₽"
                        }
       ↓
ChatGPT видит результат в AI Hub
```

---

## 🛠️ Настройка интеграции

### **1. ChatGPT Desktop → AI Hub**

**Настройка:**
- Создай MCP сервер для ChatGPT
- Подключи к `https://frogface-rpg.vercel.app/api/ai/messages`

**Пример команды:**
```javascript
// В ChatGPT MCP сервере
await fetch('https://frogface-rpg.vercel.app/api/ai/messages', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    from_agent: 'chatgpt',
    to_agent: 'rube',
    type: 'task_request',
    content: 'Создать задачу: ' + userCommand,
    priority: 'medium'
  })
});
```

### **2. Rube → AI Hub**

**Настройка:**
- Rube уже подключен через Composio
- Создай Recipe который:
  1. Читает `ai_messages` где `to_agent = 'rube'`
  2. Анализирует сообщения
  3. Создает задачи через `/api/tasks`
  4. Отвечает через `/api/ai/messages`

**Пример Recipe:**
```python
# В Rube Recipe
messages_result, _ = run_composio_tool(
    'SUPABASE_SELECT_FROM_TABLE',
    {
        'project_ref': 'ydpcfolffvatbweiuekn',
        'table': 'ai_messages',
        'select': '*',
        'filters': [
            {'column': 'to_agent', 'operator': 'eq', 'value': 'rube'},
            {'column': 'status', 'operator': 'eq', 'value': 'pending'}
        ],
        'limit': 10
    }
)

for message in messages_result.get('data', {}).get('rows', []):
    # Создаем задачу
    task_result, _ = run_composio_tool(
        'HTTP_REQUEST',
        {
            'url': 'https://frogface-rpg.vercel.app/api/tasks',
            'method': 'POST',
            'body': {
                'title': message['content'],
                'priority': message.get('priority', 'medium'),
                'projectId': 'frogface'
            }
        }
    )
    
    # Отвечаем что задача создана
    response_result, _ = run_composio_tool(
        'SUPABASE_BETA_RUN_SQL_QUERY',
        {
            'ref': 'ydpcfolffvatbweiuekn',
            'query': f'''
                INSERT INTO ai_messages (from_agent, to_agent, type, content, status)
                VALUES ('rube', 'chatgpt', 'task_response', 
                        'Задача создана: {message["content"]}', 'pending')
            '''
        }
    )
```

### **3. FrogFace Frontend → Supabase**

**Настройка:**
- ✅ Уже работает!
- Frontend автоматически:
  - Синхронизируется каждые 3 секунды: GET `/api/sync`
  - Отправляет обновления: PATCH `/api/tasks`
  - Сохраняет в localStorage как кэш

---

## 📡 API Endpoints

### **Для ChatGPT:**

```javascript
// Отправить сообщение Rube
POST /api/ai/messages
{
  from_agent: "chatgpt",
  to_agent: "rube",
  type: "task_request",
  content: "Создать задачу: ...",
  priority: "high"
}

// Прочитать ответы от Rube
GET /api/ai/messages?from_agent=rube&to_agent=chatgpt&limit=10
```

### **Для Rube:**

```python
# Прочитать новые сообщения
GET /api/ai/messages?to_agent=rube&status=pending

# Создать задачу
POST /api/tasks
{
  title: "Название задачи",
  priority: "high",
  projectId: "frogface",
  reward: 300
}

# Ответить ChatGPT
POST /api/ai/messages
{
  from_agent: "rube",
  to_agent: "chatgpt",
  type: "task_response",
  content: "Задача создана! ID: task-123"
}
```

### **Для FrogFace Frontend:**

```javascript
// Получить все задачи
GET /api/sync

// Обновить задачу (отметить как выполненную)
PATCH /api/tasks
{
  taskId: "task-123",
  completed: true
}

// Создать задачу вручную
POST /api/tasks
{
  title: "Новая задача",
  priority: "medium",
  projectId: "personal"
}
```

---

## 🎯 Примеры использования

### **Пример 1: Создание задачи через ChatGPT**

```bash
# Ты говоришь ChatGPT:
"Создай задачу с высоким приоритетом: Добавить авторизацию пользователей"

# ChatGPT отправляет в AI Hub:
{
  "from_agent": "chatgpt",
  "to_agent": "rube",
  "type": "task_request",
  "content": "Добавить авторизацию пользователей",
  "priority": "high"
}

# Rube создает задачу:
{
  "title": "Добавить авторизацию пользователей",
  "priority": "high",
  "projectId": "frogface",
  "reward": 300
}

# Ты видишь задачу в FrogFace интерфейсе! 🎉
```

### **Пример 2: Выполнение задачи**

```bash
# Ты отмечаешь задачу как выполненную в интерфейсе

# Frontend отправляет:
PATCH /api/tasks
{
  "taskId": "task-123",
  "completed": true
}

# Supabase обновляет:
tasks.completed = true

# Frontend показывает:
"✅ Задача выполнена! +300₽"

# Rube может проверить:
GET /api/ai/tasks?status=completed

# Rube уведомляет ChatGPT:
{
  "from_agent": "rube",
  "to_agent": "chatgpt",
  "type": "task_response",
  "content": "Задача 'Добавить авторизацию' выполнена! +300₽"
}
```

---

## ✅ Что уже работает

- ✅ API endpoint для обновления задач (PATCH /api/tasks)
- ✅ Frontend синхронизирует изменения в Supabase
- ✅ AI Coordination Hub через Supabase
- ✅ Rube может читать и создавать задачи
- ✅ Dashboard для мониторинга

---

## 🚀 Что еще нужно настроить

1. **ChatGPT MCP сервер** - подключить к AI Hub
2. **Rube Recipe** - автоматизировать создание задач из сообщений
3. **Автоматические уведомления** - Rube → ChatGPT при выполнении задач

---

## 📝 Резюме

**Простыми словами:**

1. **ChatGPT** слушает тебя и отправляет запросы в **AI Hub**
2. **Rube** читает запросы из **AI Hub** и создает задачи в **Supabase**
3. **FrogFace** показывает задачи из **Supabase** в интерфейсе
4. Ты отмечаешь задачи как выполненные → **Supabase** обновляется
5. **Rube** видит выполненные задачи и уведомляет **ChatGPT**

**Всё работает через Supabase как единый источник истины!** 🎉

