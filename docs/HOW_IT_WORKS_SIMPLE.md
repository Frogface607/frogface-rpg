# 🎯 Как всё работает - Простыми словами

**Объяснение интеграции ChatGPT ↔ Rube ↔ FrogFace простым языком.**

---

## 🎮 Что такое FrogFace RPG?

**FrogFace RPG** = игра для жизни, где ты зарабатываешь деньги за выполнение задач.

- ✅ Выполняешь задачу → получаешь награду в рублях
- ✅ Выполняешь все задачи за день → получаешь бонус
- ✅ Длительные стрики → больше наград
- ✅ Визуальный интерфейс с прогрессом и достижениями

---

## 🤖 Как работают три агента?

### **1. ChatGPT (Голосовой помощник)**

**Что делает:**
- Слушает твои голосовые команды
- Понимает что ты хочешь сделать
- Отправляет запросы другим агентам

**Пример:**
```
ТЫ: "Создай задачу: Добавить авторизацию"

ChatGPT: Понял! Отправляю запрос в Rube...
```

---

### **2. Rube (Автоматизатор)**

**Что делает:**
- Читает запросы от ChatGPT
- Создает задачи в FrogFace
- Автоматизирует процессы
- Отвечает ChatGPT что сделано

**Пример:**
```
Rube: Получил запрос от ChatGPT!
      Создаю задачу "Добавить авторизацию"...
      ✅ Задача создана в Supabase!
      Отвечаю ChatGPT что готово...
```

---

### **3. FrogFace (Игровой интерфейс)**

**Что делает:**
- Показывает задачи в виде квестов
- Дает награды за выполнение
- Отслеживает прогресс
- Сохраняет всё в Supabase

**Пример:**
```
FrogFace: Вижу новую задачу "Добавить авторизацию"!
          Показываю её в интерфейсе как квест с наградой 300₽
          Жду когда ты её выполнишь...
```

---

## 🔄 Полный цикл работы

### **Шаг 1: Ты создаешь задачу через ChatGPT**

```
ТЫ → ChatGPT: "Создай задачу с высоким приоритетом: Добавить авторизацию"

ChatGPT → AI Hub (Supabase): 
  Сохраняет сообщение:
  {
    "from_agent": "chatgpt",
    "to_agent": "rube",
    "type": "task_request",
    "content": "Добавить авторизацию",
    "priority": "high"
  }
```

---

### **Шаг 2: Rube читает запрос и создает задачу**

```
Rube → AI Hub: 
  Читает новые сообщения где to_agent = "rube"

Rube → Supabase:
  Создает задачу:
  {
    "id": "task-123",
    "text": "Добавить авторизацию",
    "priority": "high",
    "projectId": "frogface",
    "completed": false,
    "reward": 300
  }

Rube → AI Hub:
  Отвечает ChatGPT:
  {
    "from_agent": "rube",
    "to_agent": "chatgpt",
    "type": "task_response",
    "content": "✅ Задача создана! ID: task-123, Награда: 300₽"
  }
```

---

### **Шаг 3: FrogFace показывает задачу**

```
FrogFace → Supabase:
  Синхронизируется каждые 3 секунды: GET /api/sync

FrogFace → Интерфейс:
  Показывает новую задачу:
  📋 "Добавить авторизацию"
     🔴 HIGH
     🐸 frogface
     💰 300₽
     [ ] Отметить выполненной
```

---

### **Шаг 4: Ты выполняешь задачу**

```
ТЫ → FrogFace:
  Нажимаешь чекбокс "Отметить выполненной"

FrogFace → Supabase:
  Обновляет задачу:
  PATCH /api/tasks
  {
    "taskId": "task-123",
    "completed": true
  }

FrogFace → Интерфейс:
  ✅ Задача выполнена! +300₽
  • totalPot увеличивается на 300₽
  • completedToday увеличивается на 1
  • Показывает уведомление
```

---

### **Шаг 5: Rube видит выполненную задачу**

```
Rube → Supabase:
  Проверяет выполненные задачи:
  GET /api/tasks?completed=true

Rube → ChatGPT (через AI Hub):
  Уведомляет что задача выполнена:
  {
    "from_agent": "rube",
    "to_agent": "chatgpt",
    "type": "task_response",
    "content": "✅ Задача 'Добавить авторизацию' выполнена! +300₽"
  }

ChatGPT → ТЫ:
  Может уведомить тебя голосом или сообщением
```

---

## 🎯 Главное правило

**Все данные хранятся в Supabase** - это единый источник истины!

- ✅ ChatGPT пишет в `ai_messages`
- ✅ Rube читает `ai_messages` и создает в `tasks`
- ✅ FrogFace читает `tasks` и показывает в интерфейсе
- ✅ FrogFace обновляет `tasks` когда ты выполняешь задачи
- ✅ Rube видит изменения в `tasks` и уведомляет ChatGPT

---

## 💡 Примеры использования

### **Пример 1: Создание задачи голосом**

```
1. ТЫ: "ChatGPT, создай задачу: Исправить баг в авторизации"

2. ChatGPT отправляет в AI Hub:
   {
     "from_agent": "chatgpt",
     "to_agent": "rube",
     "type": "task_request",
     "content": "Исправить баг в авторизации",
     "priority": "high"
   }

3. Rube создает задачу в Supabase:
   {
     "id": "task-456",
     "text": "Исправить баг в авторизации",
     "priority": "high",
     "reward": 300
   }

4. FrogFace показывает задачу в интерфейсе

5. ТЫ видишь задачу и можешь её выполнить! 🎉
```

---

### **Пример 2: Выполнение задачи**

```
1. ТЫ видишь задачу в FrogFace интерфейсе
   "Исправить баг в авторизации" [ ]

2. ТЫ исправляешь баг и отмечаешь задачу:
   Клик на чекбокс → [✓]

3. FrogFace отправляет в Supabase:
   PATCH /api/tasks
   {
     "taskId": "task-456",
     "completed": true
   }

4. FrogFace обновляет UI:
   ✅ Задача выполнена! +300₽
   totalPot: 1000₽ → 1300₽

5. Rube может проверить и уведомить ChatGPT:
   "Задача 'Исправить баг в авторизации' выполнена! +300₽"
```

---

## 🛠️ Технические детали

### **API Endpoints:**

**Для ChatGPT:**
- `POST /api/ai/messages` - отправить сообщение Rube
- `GET /api/ai/messages?from_agent=rube` - прочитать ответы от Rube

**Для Rube:**
- `GET /api/ai/messages?to_agent=rube` - прочитать запросы от ChatGPT
- `POST /api/tasks` - создать задачу
- `GET /api/tasks` - получить все задачи
- `POST /api/ai/messages` - ответить ChatGPT

**Для FrogFace:**
- `GET /api/sync` - получить все задачи (синхронизация)
- `PATCH /api/tasks` - обновить задачу (отметить как выполненную)

---

### **База данных (Supabase):**

**Таблица `ai_messages`:**
- Хранит сообщения между ChatGPT и Rube
- Поля: `from_agent`, `to_agent`, `type`, `content`, `status`

**Таблица `tasks`:**
- Хранит игровые квесты
- Поля: `id`, `text`, `priority`, `completed`, `reward`, `project_id`

---

## ✅ Что уже работает

- ✅ API для обновления задач (PATCH /api/tasks)
- ✅ Frontend синхронизирует изменения в Supabase
- ✅ AI Coordination Hub через Supabase
- ✅ Rube может читать и создавать задачи
- ✅ Dashboard для мониторинга

---

## 🚀 Что нужно настроить

1. **ChatGPT MCP сервер** - подключить к AI Hub
2. **Rube Recipe** - автоматизировать создание задач
3. **Автоматические уведомления** - Rube → ChatGPT

---

## 🎉 Итог

**Простыми словами:**

1. **Ты** говоришь ChatGPT → создай задачу
2. **ChatGPT** отправляет запрос в AI Hub
3. **Rube** читает запрос и создает задачу в Supabase
4. **FrogFace** показывает задачу в интерфейсе
5. **Ты** выполняешь задачу и отмечаешь как выполненную
6. **Supabase** обновляется автоматически
7. **Rube** видит выполненную задачу и уведомляет ChatGPT

**Всё работает через Supabase как единый источник истины!** 🎉

