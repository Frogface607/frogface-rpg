# 🤖 Инструкция для Rube: FrogFace Unified Coordinator

## 🎯 ЗАДАЧА:

**Создать автоматизированного агента, который объединяет все источники задач через единый координатор.**

## 📋 ЧТО НУЖНО СДЕЛАТЬ:

### **Шаг 1: Изучи репозиторий**

Посмотри эти файлы:
- `RUBE_COORDINATOR_CODE.py` - основной координатор
- `RUBE_UNIFIED_PLAN.md` - план интеграции
- `RUBE_GITHUB_AGENT.md` - GitHub интеграция
- `openapi.json` - API схема FrogFace
- `api/tasks.js` - API endpoint для создания задач

### **Шаг 2: Создай Recipe в Rube**

**Название**: `frogface_unified_coordinator`

**Что должен делать Recipe:**

1. **Принимать задачи из разных источников:**
   - GitHub Issues (через `GITHUB_SEARCH_ISSUES_AND_PULL_REQUESTS`)
   - ChatGPT Voice (через API Tool или напрямую)
   - Gmail (опционально, если есть доступ)
   - Telegram (опционально, если есть доступ)

2. **Обрабатывать задачи:**
   - Определять приоритет (из лейблов GitHub или анализа текста)
   - Определять проект (Edison/Receptor/FrogFace/Personal)
   - Проверять на дубликаты (сравнение с существующими задачами)
   - Создавать квесты в FrogFace через API: `POST https://frogface-rpg.vercel.app/api/tasks`

3. **Сохранять mapping:**
   - Связывать source (GitHub issue, ChatGPT message) с quest_id
   - Сохранять в knowledge base для синхронизации

### **Шаг 3: Используй код из `RUBE_COORDINATOR_CODE.py`**

**Основные методы:**
- `determine_priority(source, data)` - определение приоритета
- `determine_project(data)` - определение проекта  
- `is_duplicate(title, existing_tasks)` - проверка дубликатов
- `create_quest(source, data)` - создание квеста через API

### **Шаг 4: Настрой автоматизацию**

**Вариант А: По расписанию (рекомендуется)**
- Проверять новые GitHub issues каждый час
- Создавать квесты автоматически

**Вариант Б: Webhooks (продвинуто)**
- GitHub webhook → мгновенное создание квеста

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ:

### **API Endpoints:**

**FrogFace API:**
- `POST /api/tasks` - создание задачи
  ```json
  {
    "title": "Название задачи",
    "priority": "low|medium|high|epic",
    "projectId": "edison|receptor|frogface|personal"
  }
  ```

- `GET /api/sync` - получение всех существующих задач (для дедупликации)

### **GitHub Integration:**
- `GITHUB_SEARCH_ISSUES_AND_PULL_REQUESTS` - поиск issues
- Используй query: `repo:Frogface607/frogface-rpg is:issue is:open`
- Получай labels, body, title из issues

### **Определение приоритета:**

**Из GitHub labels:**
- `bug`, `critical` → `high`
- `enhancement`, `feature` → `medium` (или `high` для feature)
- `documentation` → `low`

**Из текста (ChatGPT/Gmail/Telegram):**
- "срочно", "критично", "urgent" → `high`
- "важно", "important" → `high`
- "можно позже", "low" → `low`
- Иначе → `medium`

### **Определение проекта:**

Ищи ключевые слова в title + body:
- **Edison**: "edison", "бар", "ресторан", "персонал"
- **Receptor**: "receptor", "saas", "b2b", "платформа"
- **FrogFace**: "frogface", "rpg", "геймификация", "rube"
- **Personal**: иначе

### **Дедупликация:**

- Используй `SequenceMatcher` или LLM для сравнения title
- Threshold: 85% схожести = дубликат
- Если дубликат найден → не создавай, верни ссылку на существующий квест

## 📝 ПРИМЕР РАБОТЫ:

### **Входные данные (GitHub Issue):**
```json
{
  "title": "Протестировать интеграцию Rube → FrogFace",
  "body": "Проверить автоматическое создание квестов",
  "labels": [{"name": "enhancement"}],
  "number": 1,
  "html_url": "https://github.com/Frogface607/frogface-rpg/issues/1"
}
```

### **Обработка:**
1. Priority: `enhancement` → `medium`
2. Project: содержит "rube", "frogface" → `frogface`
3. Дубликат: проверка с существующими задачами
4. Создание квеста через API

### **Выходные данные:**
```json
{
  "success": true,
  "quest": {
    "id": "task-1762108744888-u5qvvl927",
    "title": "Протестировать интеграцию Rube → FrogFace",
    "priority": "medium",
    "project": "frogface",
    "reward": 87
  },
  "mapping": {
    "source": "github",
    "source_id": 1,
    "source_url": "https://github.com/Frogface607/frogface-rpg/issues/1",
    "quest_id": "task-1762108744888-u5qvvl927"
  }
}
```

## ✅ ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ:

После выполнения у тебя будет Recipe, который:
1. ✅ Принимает задачи из разных источников
2. ✅ Умно определяет приоритет и проект
3. ✅ Проверяет на дубликаты
4. ✅ Создает квесты в FrogFace RPG
5. ✅ Сохраняет связи для синхронизации

## 🚀 НАЧИНАЙ!

Используй код из `RUBE_COORDINATOR_CODE.py` как основу, адаптируй под Rube Recipe формат, и создай автоматизированного агента!

**Удачи! 🎯**


