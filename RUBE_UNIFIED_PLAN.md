# 🚀 Unified Agents Plan - Объединение всех агентов через Rube

## 🎯 **ГЛАВНАЯ ЦЕЛЬ:**

**Создать единую экосистему автоматизации, где все агенты работают вместе через Rube:**

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   GitHub    │────▶│             │────▶│  FrogFace   │
│   Issues    │     │    RUBE     │     │    RPG      │
└─────────────┘     │  COORDINATOR│     └─────────────┘
                    │             │
┌─────────────┐     │             │     ┌─────────────┐
│  ChatGPT    │────▶│             │────▶│ Knowledge   │
│   Voice     │     │             │     │    Base      │
└─────────────┘     └─────────────┘     └─────────────┘
                            │
┌─────────────┐             │
│   Gmail     │─────────────▶│
│   Tasks     │             │
└─────────────┘             │
                            │
┌─────────────┐             │
│  Telegram   │─────────────▶│
│  Commands   │             │
└─────────────┘             │
```

---

## 🏗️ **АРХИТЕКТУРА СИСТЕМЫ:**

### **Слой 1: Источники задач (Sources)**
- ✅ **GitHub Issues** → автоматически создает квесты
- ✅ **ChatGPT Voice** → создает квесты из голоса
- ⏳ **Gmail** → парсит письма с TODO
- ⏳ **Telegram** → команды `/quest`
- ⏳ **Google Calendar** → создает квесты из событий

### **Слой 2: Rube Coordinator (Центральный агент)**
- ✅ **Unified API Gateway** → единая точка входа для всех источников
- ✅ **Priority Manager** → определяет приоритеты из всех источников
- ✅ **Project Detector** → умное определение проекта
- ✅ **Deduplication** → предотвращает дубликаты задач
- ✅ **Mapping Service** → связывает источники с квестами

### **Слой 3: FrogFace RPG (Destination)**
- ✅ **Tasks API** → создание квестов
- ✅ **Knowledge Base** → сохранение связей issue ↔ quest
- ✅ **Stats API** → обновление статистики
- ✅ **Sync API** → синхронизация состояния

---

## 📋 **ПЛАН РЕАЛИЗАЦИИ:**

### **Фаза 1: GitHub Integration (ГОТОВО ✅)**
- ✅ Создание квестов из GitHub issues
- ✅ Определение приоритета по лейблам
- ✅ Определение проекта по содержимому
- ⏳ Завершение квестов при закрытии issue

### **Фаза 2: ChatGPT Voice Integration (ГОТОВО ✅)**
- ✅ MCP сервер для ChatGPT
- ✅ API Tool для прямых запросов
- ⏳ Интеграция через Rube для единого потока

### **Фаза 3: Rube Coordinator (В РАЗРАБОТКЕ 🔨)**
- 🔨 Создать главный Recipe
- 🔨 Unified Gateway для всех источников
- 🔨 Централизованная логика приоритетов
- 🔨 Дедупликация задач
- 🔨 Mapping service (issue ↔ quest)

### **Фаза 4: Дополнительные интеграции (ПЛАН 📝)**
- 📝 Gmail → парсинг писем с TODO
- 📝 Telegram → команды `/quest`, `/stats`
- 📝 Calendar → автоматические квесты из событий
- 📝 Notion → синхронизация проектов

---

## 🔧 **ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ:**

### **1. Главный Recipe: `frogface_unified_coordinator`**

```python
# Центральный координатор всех агентов
class FrogFaceCoordinator:
    def __init__(self):
        self.frogface_api = "https://frogface-rpg.vercel.app/api"
        self.knowledge_base = []  # Mapping issue ↔ quest
        
    def create_quest(self, source, data):
        """Создает квест из любого источника"""
        # 1. Проверка на дубликаты
        if self.is_duplicate(data['title']):
            return self.get_existing_quest(data['title'])
        
        # 2. Определение приоритета
        priority = self.determine_priority(source, data)
        
        # 3. Определение проекта
        project = self.determine_project(data)
        
        # 4. Создание квеста
        quest = self.post_quest(title=data['title'], priority=priority, project=project)
        
        # 5. Сохранение mapping
        self.save_mapping(source, quest, data)
        
        return quest
```

### **2. Unified Gateway**

```python
# Единая точка входа для всех источников
POST /api/unified/create-quest
{
    "source": "github|chatgpt|gmail|telegram",
    "data": {
        "title": "...",
        "body": "...",
        "metadata": {...}
    }
}
```

### **3. Deduplication Service**

```python
# Предотвращает дубликаты задач
def is_duplicate(title, similarity_threshold=0.85):
    """Проверяет, есть ли похожая задача"""
    existing_tasks = get_all_tasks()
    for task in existing_tasks:
        similarity = calculate_similarity(title, task['title'])
        if similarity >= similarity_threshold:
            return True
    return False
```

### **4. Priority Manager**

```python
# Умное определение приоритета из всех источников
def determine_priority(source, data):
    if source == "github":
        return get_priority_from_labels(data['labels'])
    elif source == "chatgpt":
        return get_priority_from_voice_analysis(data['voice_text'])
    elif source == "gmail":
        return get_priority_from_email_importance(data['email'])
    else:
        return "medium"
```

### **5. Project Detector**

```python
# Определение проекта из любого источника
def determine_project(data):
    text = (data.get('title', '') + ' ' + data.get('body', '')).lower()
    
    keywords = {
        'edison': ['edison', 'бар', 'ресторан', 'персонал'],
        'receptor': ['receptor', 'saas', 'b2b', 'платформа'],
        'frogface': ['frogface', 'rpg', 'геймификация', 'rube'],
        'personal': []
    }
    
    for project, words in keywords.items():
        if any(word in text for word in words):
            return project
    
    return 'personal'
```

---

## 🚀 **СЛЕДУЮЩИЕ ШАГИ:**

### **Шаг 1: Создать главный Recipe**
- Объединить GitHub и ChatGPT интеграции
- Создать единый gateway
- Добавить дедупликацию

### **Шаг 2: Протестировать поток**
- Создать issue → проверить квест
- Сказать в ChatGPT → проверить квест
- Убедиться, что нет дубликатов

### **Шаг 3: Добавить mapping service**
- Сохранять связи issue ↔ quest
- Обновлять статусы при изменении
- Синхронизировать закрытие issue → завершение квеста

### **Шаг 4: Расширить интеграции**
- Gmail
- Telegram
- Calendar

---

## 💡 **РЕКОМЕНДАЦИИ:**

### **Приоритет 1: Создать главный Recipe**
**Сейчас создадим `frogface_unified_coordinator` Recipe, который:**
- ✅ Объединяет GitHub и ChatGPT потоки
- ✅ Единая логика приоритетов и проектов
- ✅ Дедупликация задач
- ✅ Mapping service для связей

### **Приоритет 2: Протестировать**
- ✅ Создать тестовый issue
- ✅ Сказать в ChatGPT
- ✅ Проверить, что оба создают квесты без дубликатов

### **Приоритет 3: Расширить**
- ⏳ Добавить Gmail парсинг
- ⏳ Telegram команды
- ⏳ Calendar синхронизацию

---

## 🎯 **ГОТОВЫ НАЧАТЬ?**

Предлагаю прямо сейчас создать главный Recipe `frogface_unified_coordinator`! 

**Что он будет делать:**
1. Принимать задачи из любых источников (GitHub, ChatGPT, Gmail, etc.)
2. Умно определять приоритет и проект
3. Проверять на дубликаты
4. Создавать квесты в FrogFace
5. Сохранять связи для синхронизации

**Начинаем?** 🚀

